www.tgrbservice.ru {
	redir https://tgrbservice.ru{uri} permanent
}

tgrbservice.ru {
	encode gzip
	reverse_proxy app:8000

	header {
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
		-Server
	}

	log {
		output stdout
		format console
	}
}
